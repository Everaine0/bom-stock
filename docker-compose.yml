services:
  bom-stock:
    build: .
    image: bom-stock:latest
    container_name: bom-stock
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - DATA_DIR=/data
    volumes:
      - ./data:/data
